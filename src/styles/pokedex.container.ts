import { makeStyles } from  '@material-ui/core';

const useStyles = makeStyles({
    Container: {
        width: '56.25rem',
        maxWidth: '95vw',
        height: '25rem',
        maxHeight: '75%'
    }
});

export { useStyles };